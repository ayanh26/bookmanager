import React from "react";
import EditOption from "../components/EditOptions";

const Edit = (props) => {
  return <EditOption add={props.add} Bookitems={props.Bookitems} />;
};

export default Edit;
